# Cached Images Example

A Flutter project to test network_image cache

## Init app

To start add the package to `pubspec.yaml` : cached_network_image, flutter_cache_manager

Just type to terminal

```sh
flutter pub add cached_network_image flutter_cache_manager
```
